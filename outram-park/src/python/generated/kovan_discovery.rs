// @generated by codegen/gen_bindings.py from rustdoc JSON -- DO NOT EDIT.
//! Python bindings for the `kovan_discovery` backend crate.
#![allow(non_snake_case, non_camel_case_types, unused_imports,
         unreachable_patterns, clippy::all)]
use pyo3::prelude::*;
use crate::python::runtime::{from_si, to_si, err};

    // @item type:kovan_discovery::DiscoveryError
#[doc = "Errors produced by discovery / search."]
#[pyclass(name = "DiscoveryError", module = "outram_park.kovan_discovery")]
pub struct Py_kovan_discovery__DiscoveryError { pub inner: ::kovan_discovery::DiscoveryError }
#[pymethods]
impl Py_kovan_discovery__DiscoveryError {
    // @item variant:kovan_discovery::DiscoveryError::BadPattern
    #[staticmethod]
    #[pyo3(name = "BadPattern")]
    pub fn v_BadPattern(a0: String) -> Self { Self { inner: ::kovan_discovery::DiscoveryError::BadPattern(a0) } }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner { ::kovan_discovery::DiscoveryError::BadPattern(..) => "BadPattern", ::kovan_discovery::DiscoveryError::Io(..) => "Io", _ => "unknown" }
    }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __str__(&self) -> String { format!("{}", self.inner) }
}

    // @item type:kovan_discovery::FileKind
#[doc = "A category of file KOVAN cares about, with its associated extensions.\n\nThis is a closed, compile-time-known set (per the workspace's \"enums over\ntrait objects\" rule) — adding a new kind is a one-line match-exhaustiveness\nerror at every call site that needs updating, not a runtime surprise."]
#[pyclass(name = "FileKind", module = "outram_park.kovan_discovery")]
#[derive(Clone)]
pub struct Py_kovan_discovery__FileKind { pub inner: ::kovan_discovery::FileKind }
#[pymethods]
impl Py_kovan_discovery__FileKind {
    // @item method:kovan_discovery::FileKind::extensions
    #[doc = "The lowercase file extensions associated with this kind.\n\nMatching is case-insensitive: [`discover`] lowercases each candidate\nfile's extension before comparing it against this list, so `FOO.RS`\nand `foo.rs` are both recognised as [`FileKind::Source`]."]
    pub fn extensions(&self) -> Vec<String> { ::kovan_discovery::FileKind::extensions(self.inner.clone()).clone().iter().cloned().map(|e| e.clone().to_string()).collect::<Vec<_>>() }
    // @item variant:kovan_discovery::FileKind::Source
    #[staticmethod]
    #[pyo3(name = "Source")]
    pub fn v_Source() -> Self { Self { inner: ::kovan_discovery::FileKind::Source } }
    // @item variant:kovan_discovery::FileKind::Markdown
    #[staticmethod]
    #[pyo3(name = "Markdown")]
    pub fn v_Markdown() -> Self { Self { inner: ::kovan_discovery::FileKind::Markdown } }
    // @item variant:kovan_discovery::FileKind::Pdf
    #[staticmethod]
    #[pyo3(name = "Pdf")]
    pub fn v_Pdf() -> Self { Self { inner: ::kovan_discovery::FileKind::Pdf } }
    // @item variant:kovan_discovery::FileKind::Metadata
    #[staticmethod]
    #[pyo3(name = "Metadata")]
    pub fn v_Metadata() -> Self { Self { inner: ::kovan_discovery::FileKind::Metadata } }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner { ::kovan_discovery::FileKind::Source => "Source", ::kovan_discovery::FileKind::Markdown => "Markdown", ::kovan_discovery::FileKind::Pdf => "Pdf", ::kovan_discovery::FileKind::Metadata => "Metadata", _ => "unknown" }
    }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __eq__(&self, other: &Self) -> bool { self.inner == other.inner }
}

    // @item type:kovan_discovery::SearchMatch
#[doc = "A single search hit within a file.\n\nLine and column are both 1-based, matching the convention used by editors,\ncompilers, and `ripgrep`'s own CLI output (`path:line:column: text`)."]
#[pyclass(name = "SearchMatch", module = "outram_park.kovan_discovery")]
#[derive(Clone)]
pub struct Py_kovan_discovery__SearchMatch { pub inner: ::kovan_discovery::SearchMatch }
#[pymethods]
impl Py_kovan_discovery__SearchMatch {
    // @item field:kovan_discovery::SearchMatch::line
    #[getter(line)]
    pub fn get_line(&self) -> u64 { let v = self.inner.line.clone(); v }
    #[setter(line)]
    pub fn set_line(&mut self, v: u64) { self.inner.line = v; }
    // @item field:kovan_discovery::SearchMatch::column
    #[getter(column)]
    pub fn get_column(&self) -> usize { let v = self.inner.column.clone(); v }
    #[setter(column)]
    pub fn set_column(&mut self, v: usize) { self.inner.column = v; }
    // @item field:kovan_discovery::SearchMatch::text
    #[getter(text)]
    pub fn get_text(&self) -> String { let v = self.inner.text.clone(); v }
    #[setter(text)]
    pub fn set_text(&mut self, v: String) { self.inner.text = v; }
    // @item ctor:kovan_discovery::SearchMatch
    #[new]
    pub fn __new__(line: u64, column: usize, text: String) -> Self { Self { inner: ::kovan_discovery::SearchMatch { line: line, column: column, text: text } } }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __eq__(&self, other: &Self) -> bool { self.inner == other.inner }
}

    // @item type:kovan_discovery::git::BackendKind
#[doc = "Which backend a [`GitProvider`] is dispatching to."]
#[pyclass(name = "BackendKind", module = "outram_park.kovan_discovery")]
#[derive(Clone)]
pub struct Py_kovan_discovery__git__BackendKind { pub inner: ::kovan_discovery::git::BackendKind }
#[pymethods]
impl Py_kovan_discovery__git__BackendKind {
    // @item variant:kovan_discovery::git::BackendKind::Library
    #[staticmethod]
    #[pyo3(name = "Library")]
    pub fn v_Library() -> Self { Self { inner: ::kovan_discovery::git::BackendKind::Library } }
    // @item variant:kovan_discovery::git::BackendKind::Binary
    #[staticmethod]
    #[pyo3(name = "Binary")]
    pub fn v_Binary() -> Self { Self { inner: ::kovan_discovery::git::BackendKind::Binary } }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner { ::kovan_discovery::git::BackendKind::Library => "Library", ::kovan_discovery::git::BackendKind::Binary => "Binary", _ => "unknown" }
    }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __eq__(&self, other: &Self) -> bool { self.inner == other.inner }
}

    // @item type:kovan_discovery::git::CommitInfo
#[doc = "One commit, reduced to the provenance facts a knowledge layer records.\n\nAll fields are owned `String`/`i64` (no borrows into the repository), so a\n`CommitInfo` outlives the [`gix::Repository`] it was read from and can be\nstored directly on a [`kovan_common::KovanSymbol`] provenance record."]
#[pyclass(name = "CommitInfo", module = "outram_park.kovan_discovery")]
#[derive(Clone)]
pub struct Py_kovan_discovery__git__CommitInfo { pub inner: ::kovan_discovery::git::CommitInfo }
#[pymethods]
impl Py_kovan_discovery__git__CommitInfo {
    // @item field:kovan_discovery::git::CommitInfo::id
    #[getter(id)]
    pub fn get_id(&self) -> String { let v = self.inner.id.clone(); v }
    #[setter(id)]
    pub fn set_id(&mut self, v: String) { self.inner.id = v; }
    // @item field:kovan_discovery::git::CommitInfo::short_id
    #[getter(short_id)]
    pub fn get_short_id(&self) -> String { let v = self.inner.short_id.clone(); v }
    #[setter(short_id)]
    pub fn set_short_id(&mut self, v: String) { self.inner.short_id = v; }
    // @item field:kovan_discovery::git::CommitInfo::summary
    #[getter(summary)]
    pub fn get_summary(&self) -> String { let v = self.inner.summary.clone(); v }
    #[setter(summary)]
    pub fn set_summary(&mut self, v: String) { self.inner.summary = v; }
    // @item field:kovan_discovery::git::CommitInfo::author_name
    #[getter(author_name)]
    pub fn get_author_name(&self) -> String { let v = self.inner.author_name.clone(); v }
    #[setter(author_name)]
    pub fn set_author_name(&mut self, v: String) { self.inner.author_name = v; }
    // @item field:kovan_discovery::git::CommitInfo::author_email
    #[getter(author_email)]
    pub fn get_author_email(&self) -> String { let v = self.inner.author_email.clone(); v }
    #[setter(author_email)]
    pub fn set_author_email(&mut self, v: String) { self.inner.author_email = v; }
    // @item field:kovan_discovery::git::CommitInfo::committed_unix_seconds
    #[getter(committed_unix_seconds)]
    pub fn get_committed_unix_seconds(&self) -> i64 { let v = self.inner.committed_unix_seconds.clone(); v }
    #[setter(committed_unix_seconds)]
    pub fn set_committed_unix_seconds(&mut self, v: i64) { self.inner.committed_unix_seconds = v; }
    // @item ctor:kovan_discovery::git::CommitInfo
    #[new]
    pub fn __new__(id: String, short_id: String, summary: String, author_name: String, author_email: String, committed_unix_seconds: i64) -> Self { Self { inner: ::kovan_discovery::git::CommitInfo { id: id, short_id: short_id, summary: summary, author_name: author_name, author_email: author_email, committed_unix_seconds: committed_unix_seconds } } }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __eq__(&self, other: &Self) -> bool { self.inner == other.inner }
}

    // @item type:kovan_discovery::git::GitError
#[doc = "Errors from the git-awareness layer."]
#[pyclass(name = "GitError", module = "outram_park.kovan_discovery")]
pub struct Py_kovan_discovery__git__GitError { pub inner: ::kovan_discovery::git::GitError }
#[pymethods]
impl Py_kovan_discovery__git__GitError {
    // @item variant:kovan_discovery::git::GitError::NotARepository
    #[staticmethod]
    #[pyo3(name = "NotARepository")]
    pub fn v_NotARepository(a0: String) -> Self { Self { inner: ::kovan_discovery::git::GitError::NotARepository(std::path::PathBuf::from(a0)) } }
    // @item variant:kovan_discovery::git::GitError::Open
    #[staticmethod]
    #[pyo3(name = "Open")]
    pub fn v_Open(a0: String) -> Self { Self { inner: ::kovan_discovery::git::GitError::Open(a0) } }
    // @item variant:kovan_discovery::git::GitError::UnbornHead
    #[staticmethod]
    #[pyo3(name = "UnbornHead")]
    pub fn v_UnbornHead() -> Self { Self { inner: ::kovan_discovery::git::GitError::UnbornHead } }
    // @item variant:kovan_discovery::git::GitError::Backend
    #[staticmethod]
    #[pyo3(name = "Backend")]
    pub fn v_Backend(a0: String) -> Self { Self { inner: ::kovan_discovery::git::GitError::Backend(a0) } }
    // @item variant:kovan_discovery::git::GitError::BinaryUnavailableOnTarget
    #[staticmethod]
    #[pyo3(name = "BinaryUnavailableOnTarget")]
    pub fn v_BinaryUnavailableOnTarget() -> Self { Self { inner: ::kovan_discovery::git::GitError::BinaryUnavailableOnTarget } }
    // @item variant:kovan_discovery::git::GitError::BinaryFailed
    #[staticmethod]
    #[pyo3(name = "BinaryFailed")]
    pub fn v_BinaryFailed(a0: String) -> Self { Self { inner: ::kovan_discovery::git::GitError::BinaryFailed(a0) } }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner { ::kovan_discovery::git::GitError::NotARepository(..) => "NotARepository", ::kovan_discovery::git::GitError::Open(..) => "Open", ::kovan_discovery::git::GitError::UnbornHead => "UnbornHead", ::kovan_discovery::git::GitError::Backend(..) => "Backend", ::kovan_discovery::git::GitError::Unsupported(..) => "Unsupported", ::kovan_discovery::git::GitError::BinaryUnavailableOnTarget => "BinaryUnavailableOnTarget", ::kovan_discovery::git::GitError::BinaryFailed(..) => "BinaryFailed", _ => "unknown" }
    }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __str__(&self) -> String { format!("{}", self.inner) }
}

    // @item type:kovan_discovery::git::GixCliBackend
#[doc = "The **fallback** git backend, which shells out to the `gix` command-line\ntool (installed by `kovan setup`).\n\nThis exists to satisfy KOVAN's \"library first, fall back on the binary\"\nprinciple when the [`GixBackend`] library path cannot open a repository. The\n`gix` CLI's structured porcelain is still experimental and its text output\nis not a stable contract, so the structured queries here deliberately return\n[`GitError::Unsupported`] rather than parse unstable output — the library\nbackend is authoritative for those. What the binary backend *does*\nguarantee is availability detection ([`GixCliBackend::is_available`]).\n\nOn `target_os = \"android\"` there is no `gix` executable, so every operation\nreturns [`GitError::BinaryUnavailableOnTarget`]."]
#[pyclass(name = "GixCliBackend", module = "outram_park.kovan_discovery")]
pub struct Py_kovan_discovery__git__GixCliBackend { pub inner: ::kovan_discovery::git::GixCliBackend }
#[pymethods]
impl Py_kovan_discovery__git__GixCliBackend {
    // @item method:kovan_discovery::git::GixCliBackend::new
    #[doc = "A binary backend rooted at `root`, invoking the default `gix` program\nname (resolved on `PATH`)."]
    #[new]
    pub fn new(root: String) -> Py_kovan_discovery__git__GixCliBackend { Py_kovan_discovery__git__GixCliBackend { inner: ::kovan_discovery::git::GixCliBackend::new(std::path::Path::new(&root)) } }
    // @item method:kovan_discovery::git::GixCliBackend::with_program
    #[doc = "A binary backend that invokes `program` instead of the default `gix`\n(e.g. an absolute path to the executable)."]
    #[staticmethod]
    pub fn with_program(root: String, program: String) -> Py_kovan_discovery__git__GixCliBackend { Py_kovan_discovery__git__GixCliBackend { inner: ::kovan_discovery::git::GixCliBackend::with_program(std::path::Path::new(&root), program) } }
    // @item method:kovan_discovery::git::GixCliBackend::is_available
    #[doc = "Whether the `gix` executable is runnable — probes `gix --version`.\nAlways `false` on Android (no binary on that target)."]
    pub fn is_available(&self) -> bool { ::kovan_discovery::git::GixCliBackend::is_available(&self.inner) }
    // @item method:kovan_discovery::git::GixCliBackend::version
    #[doc = "The `gix --version` string, or an error if the binary can't be run.\n\nThis is the one operation the binary backend performs by actually\nshelling out; it is how [`GitProvider::open_preferring_library`] decides\nwhether a binary fallback is usable."]
    pub fn version(&self) -> PyResult<String> { err(::kovan_discovery::git::GixCliBackend::version(&self.inner)).map(|v| v) }
}

    // @item type:kovan_discovery::git::HeadInfo
#[doc = "The state of `HEAD`: which branch is checked out and the commit it resolves\nto."]
#[pyclass(name = "HeadInfo", module = "outram_park.kovan_discovery")]
#[derive(Clone)]
pub struct Py_kovan_discovery__git__HeadInfo { pub inner: ::kovan_discovery::git::HeadInfo }
#[pymethods]
impl Py_kovan_discovery__git__HeadInfo {
    // @item field:kovan_discovery::git::HeadInfo::branch
    #[getter(branch)]
    pub fn get_branch(&self) -> Option<String> { let v = self.inner.branch.clone(); v.map(|e| e) }
    #[setter(branch)]
    pub fn set_branch(&mut self, v: Option<String>) { self.inner.branch = v.map(|e| e); }
    // @item field:kovan_discovery::git::HeadInfo::detached
    #[getter(detached)]
    pub fn get_detached(&self) -> bool { let v = self.inner.detached.clone(); v }
    #[setter(detached)]
    pub fn set_detached(&mut self, v: bool) { self.inner.detached = v; }
    // @item field:kovan_discovery::git::HeadInfo::commit
    #[getter(commit)]
    pub fn get_commit(&self) -> Py_kovan_discovery__git__CommitInfo { let v = self.inner.commit.clone(); Py_kovan_discovery__git__CommitInfo { inner: v } }
    #[setter(commit)]
    pub fn set_commit(&mut self, v: Py_kovan_discovery__git__CommitInfo) { self.inner.commit = v.inner; }
    // @item ctor:kovan_discovery::git::HeadInfo
    #[new]
    pub fn __new__(branch: Option<String>, detached: bool, commit: Py_kovan_discovery__git__CommitInfo) -> Self { Self { inner: ::kovan_discovery::git::HeadInfo { branch: branch.map(|e| e), detached: detached, commit: commit.inner } } }
    pub fn __repr__(&self) -> String { format!("{:?}", self.inner) }
    pub fn __eq__(&self, other: &Self) -> bool { self.inner == other.inner }
}

    // @item fn:kovan_discovery::discover_kind
#[doc = "Discover all files under `root` of a given [`FileKind`]. Equivalent to\ncalling [`discover`] with that kind's [`FileKind::extensions`].\n\nSee [`discover`] for the `.gitignore` and determinism (sorted-output)\nguarantees, which apply identically here."]
#[pyfunction(name = "discover_kind")]
pub fn fn_kovan_discovery__discover_kind(root: String, kind: Py_kovan_discovery__FileKind) -> Vec<String> { ::kovan_discovery::discover_kind(std::path::Path::new(&root), kind.inner).into_iter().map(|e| e.to_string_lossy().into_owned()).collect::<Vec<_>>() }

    // @item fn:kovan_discovery::search_file
#[doc = "Search a single file for `pattern` (a regular expression), returning every\nmatching line with its line number and column. Uses the ripgrep engine\n(`grep-searcher` + `grep-regex`), so the pattern accepts the same syntax\n`rg` does (Rust's `regex` crate syntax).\n\n# Determinism\n\nMatches are returned in ascending line order (the order the file is read\ntop to bottom); re-running against an unchanged file always yields an\nidentical result.\n\n# Errors\n\n- [`DiscoveryError::BadPattern`] if `pattern` fails to compile as a regex.\n- [`DiscoveryError::Io`] if `path` cannot be opened/read, or is not valid\n  UTF-8 (this function searches decoded text; binary files are rejected\n  rather than silently mis-decoded).\n\n# Column computation\n\n[`SearchMatch::column`] is derived by re-locating the pattern within the\nmatched line via [`grep_matcher::Matcher::find`]. This is a second, local\nsearch over an already-matched line (not a full extra file pass), so it is\ncheap. In the practically-unreachable case where that re-location fails —\nthe searcher matched a line but the matcher then reports no match on that\nsame line — the column falls back to `1` rather than panicking."]
#[pyfunction(name = "search_file")]
pub fn fn_kovan_discovery__search_file(path: String, pattern: String) -> PyResult<Vec<Py_kovan_discovery__SearchMatch>> { err(::kovan_discovery::search_file(std::path::Path::new(&path), &pattern)).map(|v| v.into_iter().map(|e| Py_kovan_discovery__SearchMatch { inner: e }).collect::<Vec<_>>()) }

    // @item fn:kovan_discovery::search_repository
#[doc = "Discover every file of `kind` under `root`, then [`search_file`] each one\nfor `pattern`. The combined \"find files, then grep them\" primitive —\n`kovan-semantics`'s `rough_definition_scan` implements this same\ndiscover-then-search loop by hand today; reach for this directly if you\njust need \"grep this whole repository for X\" without a language-specific\nheuristic on top.\n\n# Determinism\n\nFiles are visited in the sorted order [`discover_kind`] returns, and each\nfile's matches are appended in ascending line order, so the overall\n`Vec<(PathBuf, SearchMatch)>` is fully deterministic for a given tree.\n\n# Errors\n\nStops at (and returns) the first [`DiscoveryError`] raised by\n[`search_file`] on any discovered file — a [`DiscoveryError::BadPattern`]\non the first file means every subsequent file has the same bad pattern, so\nfailing fast avoids repeating the same compile error. A per-file I/O error\n(e.g. one non-UTF-8 file among many source files) aborts the whole scan\nrather than silently skipping that file; callers that want best-effort\nbehaviour should call [`discover_kind`] and [`search_file`] themselves and\ndecide how to handle a single file's error."]
#[pyfunction(name = "search_repository")]
pub fn fn_kovan_discovery__search_repository(root: String, kind: Py_kovan_discovery__FileKind, pattern: String) -> PyResult<Vec<(String, Py_kovan_discovery__SearchMatch)>> { err(::kovan_discovery::search_repository(std::path::Path::new(&root), kind.inner, &pattern)).map(|v| v.into_iter().map(|e| { let (e0, e1) = e; (e0.to_string_lossy().into_owned(), Py_kovan_discovery__SearchMatch { inner: e1 }) }).collect::<Vec<_>>()) }

pub fn register(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_class::<Py_kovan_discovery__DiscoveryError>()?;
    m.add_class::<Py_kovan_discovery__FileKind>()?;
    m.add_class::<Py_kovan_discovery__SearchMatch>()?;
    m.add_class::<Py_kovan_discovery__git__BackendKind>()?;
    m.add_class::<Py_kovan_discovery__git__CommitInfo>()?;
    m.add_class::<Py_kovan_discovery__git__GitError>()?;
    m.add_class::<Py_kovan_discovery__git__GixCliBackend>()?;
    m.add_class::<Py_kovan_discovery__git__HeadInfo>()?;
    m.add_function(wrap_pyfunction!(fn_kovan_discovery__discover_kind, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_discovery__search_file, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_discovery__search_repository, m)?)?;
    Ok(())
}
