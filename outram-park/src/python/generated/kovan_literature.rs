// @generated by codegen/gen_bindings.py from rustdoc JSON -- DO NOT EDIT.
//! Python bindings for the `kovan_literature` backend crate.
#![allow(
    non_snake_case,
    non_camel_case_types,
    unused_imports,
    unreachable_patterns,
    clippy::all
)]
use pyo3::prelude::*;
use crate::python::runtime::{err, from_si, to_si};

// @item type:kovan_literature::BibEntry
#[doc = "One parsed `.bib` entry (op-vi1n) — the entry type (`article`,\n`techreport`, …, lowercased), its cite key, and every field it declares.\n\nField names are lowercased (BibTeX field names are case-insensitive) and\nvalues have their delimiters (`{}` or `\"...\"`) stripped but are otherwise\nverbatim — see the module doc's \"deliberately one-way and shallow\" note."]
#[pyclass(name = "BibEntry", module = "outram_park.kovan_literature")]
#[derive(Clone)]
pub struct Py_kovan_literature__BibEntry {
    pub inner: ::kovan_literature::BibEntry,
}
#[pymethods]
impl Py_kovan_literature__BibEntry {
    // @item field:kovan_literature::BibEntry::entry_type
    #[getter(entry_type)]
    pub fn get_entry_type(&self) -> String {
        let v = self.inner.entry_type.clone();
        v
    }
    #[setter(entry_type)]
    pub fn set_entry_type(&mut self, v: String) {
        self.inner.entry_type = v;
    }
    // @item field:kovan_literature::BibEntry::cite_key
    #[getter(cite_key)]
    pub fn get_cite_key(&self) -> String {
        let v = self.inner.cite_key.clone();
        v
    }
    #[setter(cite_key)]
    pub fn set_cite_key(&mut self, v: String) {
        self.inner.cite_key = v;
    }
    pub fn __repr__(&self) -> String {
        format!("{:?}", self.inner)
    }
    pub fn __eq__(&self, other: &Self) -> bool {
        self.inner == other.inner
    }
}

// @item type:kovan_literature::BibParseError
#[doc = "Errors from [`parse_bib_entries`]."]
#[pyclass(name = "BibParseError", module = "outram_park.kovan_literature")]
#[derive(Clone)]
pub struct Py_kovan_literature__BibParseError {
    pub inner: ::kovan_literature::BibParseError,
}
#[pymethods]
impl Py_kovan_literature__BibParseError {
    // @item variant:kovan_literature::BibParseError::UnterminatedEntry
    #[staticmethod]
    #[pyo3(name = "UnterminatedEntry")]
    pub fn v_UnterminatedEntry(entry_type: String, near: String) -> Self {
        Self {
            inner: ::kovan_literature::BibParseError::UnterminatedEntry {
                entry_type: entry_type,
                near: near,
            },
        }
    }
    // @item variant:kovan_literature::BibParseError::MissingCiteKey
    #[staticmethod]
    #[pyo3(name = "MissingCiteKey")]
    pub fn v_MissingCiteKey(entry_type: String) -> Self {
        Self {
            inner: ::kovan_literature::BibParseError::MissingCiteKey {
                entry_type: entry_type,
            },
        }
    }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner {
            ::kovan_literature::BibParseError::UnterminatedEntry { .. } => "UnterminatedEntry",
            ::kovan_literature::BibParseError::MissingCiteKey { .. } => "MissingCiteKey",
            _ => "unknown",
        }
    }
    pub fn __repr__(&self) -> String {
        format!("{:?}", self.inner)
    }
    pub fn __str__(&self) -> String {
        format!("{}", self.inner)
    }
    pub fn __eq__(&self, other: &Self) -> bool {
        self.inner == other.inner
    }
}

// @item type:kovan_literature::Heading
#[doc = "One heading in a Markdown document: its level (1–6) and text."]
#[pyclass(name = "Heading", module = "outram_park.kovan_literature")]
#[derive(Clone)]
pub struct Py_kovan_literature__Heading {
    pub inner: ::kovan_literature::Heading,
}
#[pymethods]
impl Py_kovan_literature__Heading {
    // @item field:kovan_literature::Heading::level
    #[getter(level)]
    pub fn get_level(&self) -> u8 {
        let v = self.inner.level.clone();
        v
    }
    #[setter(level)]
    pub fn set_level(&mut self, v: u8) {
        self.inner.level = v;
    }
    // @item field:kovan_literature::Heading::text
    #[getter(text)]
    pub fn get_text(&self) -> String {
        let v = self.inner.text.clone();
        v
    }
    #[setter(text)]
    pub fn set_text(&mut self, v: String) {
        self.inner.text = v;
    }
    // @item ctor:kovan_literature::Heading
    #[new]
    pub fn __new__(level: u8, text: String) -> Self {
        Self {
            inner: ::kovan_literature::Heading {
                level: level,
                text: text,
            },
        }
    }
    pub fn __repr__(&self) -> String {
        format!("{:?}", self.inner)
    }
    pub fn __eq__(&self, other: &Self) -> bool {
        self.inner == other.inner
    }
}

// @item type:kovan_literature::LiteratureError
#[doc = "Errors produced by the literature pipeline."]
#[pyclass(name = "LiteratureError", module = "outram_park.kovan_literature")]
#[derive(Clone)]
pub struct Py_kovan_literature__LiteratureError {
    pub inner: ::kovan_literature::LiteratureError,
}
#[pymethods]
impl Py_kovan_literature__LiteratureError {
    // @item variant:kovan_literature::LiteratureError::Io
    #[staticmethod]
    #[pyo3(name = "Io")]
    pub fn v_Io(a0: String) -> Self {
        Self {
            inner: ::kovan_literature::LiteratureError::Io(a0),
        }
    }
    /// The name of the enum variant this value holds.
    pub fn variant(&self) -> &'static str {
        match &self.inner {
            ::kovan_literature::LiteratureError::Unimplemented(..) => "Unimplemented",
            ::kovan_literature::LiteratureError::Io(..) => "Io",
            _ => "unknown",
        }
    }
    pub fn __repr__(&self) -> String {
        format!("{:?}", self.inner)
    }
    pub fn __str__(&self) -> String {
        format!("{}", self.inner)
    }
    pub fn __eq__(&self, other: &Self) -> bool {
        self.inner == other.inner
    }
}

// @item fn:kovan_literature::extract_assets
#[doc = "Extract embedded raster images from a source PDF into `out_dir`, returning\nthe written file paths in deterministic (PDF object-id) order.\n\nScope (best-effort): only images whose PDF filter is already a standalone\nfile format are written — `DCTDecode` (JPEG, `.jpg`) and `JPXDecode`\n(JPEG-2000, `.jp2`) — because their stream bytes *are* the encoded file.\nImages stored under other filters (e.g. raw `FlateDecode` samples that would\nneed re-encoding to PNG with the right colour space/bit depth) are skipped,\nnot fabricated. `out_dir` is created only if at least one image is written.\n\nFiles are named `<pdf-stem>-imgNNN.<ext>`. Deterministic and offline."]
#[pyfunction(name = "extract_assets")]
pub fn fn_kovan_literature__extract_assets(pdf: String, out_dir: String) -> PyResult<Vec<String>> {
    err(::kovan_literature::extract_assets(
        std::path::Path::new(&pdf),
        std::path::Path::new(&out_dir),
    ))
    .map(|v| {
        v.into_iter()
            .map(|e| e.to_string_lossy().into_owned())
            .collect::<Vec<_>>()
    })
}

// @item fn:kovan_literature::extract_metadata
#[cfg(feature = "kovan-common")]
#[doc = "Extract document metadata from a source PDF into a [`KovanDocument`].\n\nSee the module docs for the heuristic order. Returns\n[`LiteratureError::Io`] only when the PDF can be neither text-extracted nor\nparsed at all; a partially-readable PDF yields a document with whatever\nfields were recoverable and empty/`None` for the rest.\n\nDeterministic and offline. The returned document's `markdown_body` is the\ngenerated Markdown (so a single call yields both the metadata and the body)."]
#[pyfunction(name = "extract_metadata")]
pub fn fn_kovan_literature__extract_metadata(
    pdf: String,
) -> PyResult<crate::python::generated::kovan_common::Py_kovan_common__KovanDocument> {
    err(::kovan_literature::extract_metadata(std::path::Path::new(
        &pdf,
    )))
    .map(|v| crate::python::generated::kovan_common::Py_kovan_common__KovanDocument { inner: v })
}

// @item fn:kovan_literature::extract_pdf_text
#[doc = "Extract the raw plain text of a PDF, in reading order, as `pdf-extract`\nreports it (pages separated by form-feed `U+000C`).\n\nDeterministic and offline. Returns [`LiteratureError::Io`] if the file cannot\nbe parsed, and — because `pdf-extract` panics on some malformed inputs —\nconverts any internal panic into the same error rather than unwinding into\nthe caller."]
#[pyfunction(name = "extract_pdf_text")]
pub fn fn_kovan_literature__extract_pdf_text(pdf: String) -> PyResult<String> {
    err(::kovan_literature::extract_pdf_text(std::path::Path::new(
        &pdf,
    )))
    .map(|v| v)
}

// @item fn:kovan_literature::markdown_outline
#[doc = "Parse a Markdown body and return its heading outline, using `pulldown-cmark`\n(the one Markdown parser KOVAN starts with, `docs/kovan.md`). Deterministic;\nuseful for building a table of contents or for splitting long documents.\n\nInput: any CommonMark string. Output: headings in document order."]
#[pyfunction(name = "markdown_outline")]
pub fn fn_kovan_literature__markdown_outline(
    markdown: String,
) -> Vec<Py_kovan_literature__Heading> {
    ::kovan_literature::markdown_outline(&markdown)
        .into_iter()
        .map(|e| Py_kovan_literature__Heading { inner: e })
        .collect::<Vec<_>>()
}

// @item fn:kovan_literature::parse_bib_entries
#[doc = "Parse a `.bib` file's text into its entries (op-vi1n).\n\nHandles the common `@type{key, field = {value}, field2 = \"value2\", year =\n2020,}` shape, including nested braces inside a `{…}` value (e.g. `title =\n{Heat {and} mass transfer}`) and a trailing comma before the closing `}`.\nText outside any `@…{…}` block (blank lines, `%` comments) is ignored, the\nsame way BibTeX itself ignores it.\n\n`@string{…}`, `@preamble{…}` and `@comment{…}` blocks (case-insensitive)\nare recognised, brace-balanced past, and **dropped** — they are BibTeX\nmacro/comment constructs, not citable entries, and don't have the\n`key, field = value, …` shape this parser otherwise expects.\n\n# Errors\n\n[`BibParseError::UnterminatedEntry`] if a `{` (entry or field value) is\nnever closed. [`BibParseError::MissingCiteKey`] if an ordinary entry's\nbody doesn't start with `<key> ,`."]
#[pyfunction(name = "parse_bib_entries")]
pub fn fn_kovan_literature__parse_bib_entries(
    input: String,
) -> PyResult<Vec<Py_kovan_literature__BibEntry>> {
    err(::kovan_literature::parse_bib_entries(&input)).map(|v| {
        v.into_iter()
            .map(|e| Py_kovan_literature__BibEntry { inner: e })
            .collect::<Vec<_>>()
    })
}

// @item fn:kovan_literature::pdf_to_markdown
#[doc = "Import a PDF and produce a structured Markdown body.\n\nPipeline: [`extract_pdf_text`] → [`crate::text_to_markdown`]. The result is a\nsingle Markdown string; split it with [`crate::split_markdown_by_page_limit`]\nto keep each generated document within [`crate::MAX_MARKDOWN_PAGES`].\n\nDeterministic and offline."]
#[pyfunction(name = "pdf_to_markdown")]
pub fn fn_kovan_literature__pdf_to_markdown(pdf: String) -> PyResult<String> {
    err(::kovan_literature::pdf_to_markdown(std::path::Path::new(
        &pdf,
    )))
    .map(|v| v)
}

// @item fn:kovan_literature::render_entries
#[doc = "Render a whole `.bib` file's worth of entries — one blank line between\nentries, matching common `.bib` file style."]
#[pyfunction(name = "render_entries")]
pub fn fn_kovan_literature__render_entries(entries: Vec<Py_kovan_literature__BibEntry>) -> String {
    ::kovan_literature::render_entries(&entries.into_iter().map(|e| e.inner).collect::<Vec<_>>())
}

// @item fn:kovan_literature::render_entry
#[doc = "Render one [`BibEntry`] back to BibTeX text — the write side of a\nJabRef-like entry editor (op-xj8t), sitting alongside [`parse_bib_entries`]\nrather than [`to_bibtex`] (which needs a full [`KovanDocument`], not a raw\nentry read back from an arbitrary `.bib` file).\n\nFields are written in `entry.fields`' `BTreeMap` order (alphabetical by\nfield name), **not** the original file's field order — round-tripping a\nparsed-then-rendered entry byte-for-byte is not a goal (see the module\ndoc's \"deliberately one-way and shallow\" note); the entry's *content* is\npreserved, its formatting is not."]
#[pyfunction(name = "render_entry")]
pub fn fn_kovan_literature__render_entry(
    entry: PyRef<'_, Py_kovan_literature__BibEntry>,
) -> String {
    ::kovan_literature::render_entry(&entry.inner)
}

// @item fn:kovan_literature::split_markdown_by_page_limit
#[doc = "Split a Markdown body produced by [`text_to_markdown`] into chunks that each\ncontain at most `max_pages` source pages, so no generated document exceeds\nthe `docs/kovan.md` `≤ 30`-page target (see [`crate::MAX_MARKDOWN_PAGES`]).\n\nSplitting happens on the [`PAGE_SEPARATOR`] boundaries the converter\ninserted. A body with `≤ max_pages` pages returns as a single chunk. A\n`max_pages` of 0 is treated as 1."]
#[pyfunction(name = "split_markdown_by_page_limit")]
pub fn fn_kovan_literature__split_markdown_by_page_limit(
    markdown: String,
    max_pages: u32,
) -> Vec<String> {
    ::kovan_literature::split_markdown_by_page_limit(&markdown, max_pages)
        .into_iter()
        .map(|e| e)
        .collect::<Vec<_>>()
}

// @item fn:kovan_literature::storage::document_type_from_path
#[cfg(feature = "kovan-common")]
#[doc = "Infer a [`super::DocumentType`] from a storage sub-directory name in the\nsource path (`papers/`, `reports/`, `standards/`, `benchmarks/`,\n`manuals/`, `theses/` or `dissertations/`), falling back to\n[`super::DocumentType::Other`] when none is present."]
#[pyfunction(name = "document_type_from_path")]
pub fn fn_kovan_literature__storage__document_type_from_path(
    path: String,
) -> crate::python::generated::kovan_common::Py_kovan_common__DocumentType {
    crate::python::generated::kovan_common::Py_kovan_common__DocumentType {
        inner: ::kovan_literature::storage::document_type_from_path(std::path::Path::new(&path)),
    }
}

// @item fn:kovan_literature::storage::generated_dir_for
#[cfg(feature = "kovan-common")]
#[doc = "Directory a generated artifact of `kind` and `visibility` belongs in,\njoined onto `base` (usually the `kovan-literature` crate directory).\n\nThe generated tree is split by [`Visibility`] one level below each artifact\nkind — `generated/bibtex/open/`, `generated/bibtex/proprietary/`, and so on\n— because the two halves have different distribution rules:\n\n- **open** — committed to the repository, and for BibTeX also *published*\n  in the packaged crate (citation entries are small bibliographic facts).\n- **proprietary** — never committed and never published; an artifact\n  derived from user-owned content is equally user-owned.\n\nBoth rules are enforced outside this function (the root `.gitignore` and\nthe `exclude` list in this crate's `Cargo.toml`); this is the single place\nthat decides *which* directory a writer should target, so the two\nmechanisms and the code cannot drift apart.\n\n`kind` should be one of [`BIBTEX_DIR`], [`MARKDOWN_DIR`], [`ASSETS_DIR`]."]
#[pyfunction(name = "generated_dir_for")]
pub fn fn_kovan_literature__storage__generated_dir_for(
    base: String,
    kind: String,
    visibility: crate::python::generated::kovan_common::Py_kovan_common__Visibility,
) -> String {
    ::kovan_literature::storage::generated_dir_for(
        std::path::Path::new(&base),
        &kind,
        visibility.inner,
    )
    .to_string_lossy()
    .into_owned()
}

// @item fn:kovan_literature::storage::root_for
#[cfg(feature = "kovan-common")]
#[doc = "Return the storage root for a given [`Visibility`], joined onto `base`\n(usually the `kovan-literature` crate directory)."]
#[pyfunction(name = "root_for")]
pub fn fn_kovan_literature__storage__root_for(
    base: String,
    visibility: crate::python::generated::kovan_common::Py_kovan_common__Visibility,
) -> String {
    ::kovan_literature::storage::root_for(std::path::Path::new(&base), visibility.inner)
        .to_string_lossy()
        .into_owned()
}

// @item fn:kovan_literature::storage::visibility_from_path
#[cfg(feature = "kovan-common")]
#[doc = "Infer a document's [`Visibility`] from where its source file lives.\n\n**Closed by default.** A document is [`Visibility::Open`] only when its\npath explicitly contains an `open/` component. Everything else —\nincluding `proprietary/`, and including any path with neither marker —\nis [`Visibility::Proprietary`].\n\n# Why the default is closed\n\nThe two ways of being wrong here are not symmetric:\n\n- Mislabelling an **open** document as proprietary costs a reviewer a\n  minute and keeps a committable file out of git. Recoverable.\n- Mislabelling a **proprietary** document as open invites it into\n  `open/`, which `.gitignore` deliberately un-ignores for PDFs, and from\n  there into a public repository. That is a licence violation, and\n  pushed history is not something you can quietly take back.\n\nSo the rule fails towards the recoverable error. This matches the\ninstruction in `kovan_import/README.md` — \"unsure -> treat as\nproprietary and ask\" — and `DATA_POLICY.md`.\n\n# The bug this replaced\n\nUntil 2026-08-11 this defaulted to [`Visibility::Open`] and only\nspecial-cased `proprietary/`, so a source file staged anywhere else —\nnotably `kovan_import/`, the gitignored drop area where documents sit\n*be"]
#[pyfunction(name = "visibility_from_path")]
pub fn fn_kovan_literature__storage__visibility_from_path(
    path: String,
) -> crate::python::generated::kovan_common::Py_kovan_common__Visibility {
    crate::python::generated::kovan_common::Py_kovan_common__Visibility {
        inner: ::kovan_literature::storage::visibility_from_path(std::path::Path::new(&path)),
    }
}

// @item fn:kovan_literature::text_to_markdown
#[doc = "Convert the flat text an extractor produces into structured Markdown.\n\nThis is a deterministic, offline transform. Its steps:\n\n1. Split on the form-feed (`U+000C`) page breaks emitted by `pdf-extract`.\n2. Within each page, group runs of non-blank lines into paragraphs (a blank\n   line ends a paragraph), joining wrapped lines with a space and undoing\n   end-of-line hyphenation (`combus-\\ntion` → `combustion`).\n3. Promote lines that are *high-confidence* headings to Markdown `##`+:\n   numbered section headers (`1 Introduction`, `2.1 Methods`) and a fixed\n   set of well-known section keywords (`Abstract`, `References`, …). Nesting\n   depth sets the level. Everything else stays paragraph text — the\n   converter deliberately does **not** guess at other structure, to avoid\n   fabricating headings the source does not have.\n4. Join pages with [`PAGE_SEPARATOR`].\n\nReturns a single Markdown body. Use [`split_markdown_by_page_limit`] to break\nit into `≤ MAX_MARKDOWN_PAGES` chunks."]
#[pyfunction(name = "text_to_markdown")]
pub fn fn_kovan_literature__text_to_markdown(raw: String) -> String {
    ::kovan_literature::text_to_markdown(&raw)
}

// @item fn:kovan_literature::to_bibtex
#[cfg(feature = "kovan-common")]
#[doc = "Render a [`KovanDocument`] to a single BibTeX entry.\n\n## Entry-type mapping (from [`DocumentType`])\n\n| `DocumentType` | BibTeX entry |\n|---|---|\n| `Paper` | `@article` |\n| `Report` | `@techreport` |\n| `Manual` | `@manual` |\n| `Thesis` | `@phdthesis` |\n| `Standard` | `@misc` |\n| `Benchmark` | `@misc` |\n| `Other` | `@misc` |\n\n`Standard`/`Benchmark` map to `@misc` for portability — classic BibTeX has no\n`@standard`/`@benchmark`, and `@misc` accepts arbitrary fields, so no data is\nlost. (biblatex users can post-process the type.)\n\n## Fields\n\nThe citation key is the document `slug`, sanitised to `[A-Za-z0-9:_-]`. Every\npresent field is emitted: `author` (as `Family, Given and …`), `title`,\n`year`, `journal`, `volume`, `number`, `pages`, `institution`, `publisher`,\n`doi`, `url` (from `source_url`), `keywords`, and `abstract`. Absent optional\nfields are simply omitted. All values are TeX-escaped (see `escape_tex`).\n\nOne field is spelled per entry type: `institution` becomes **`school`** on a\n`@phdthesis`, which is the field BibTeX styles read for the awarding\nuniversity.\n\nThe output is deterministic: field order is fixed and independent of the\ndocument's construction path."]
#[pyfunction(name = "to_bibtex")]
pub fn fn_kovan_literature__to_bibtex(
    doc: PyRef<'_, crate::python::generated::kovan_common::Py_kovan_common__KovanDocument>,
) -> String {
    ::kovan_literature::to_bibtex(&doc.inner)
}

// @item const:kovan_literature::MAX_MARKDOWN_PAGES
// @item const:kovan_literature::PAGE_SEPARATOR
// @item const:kovan_literature::storage::ASSETS_DIR
// @item const:kovan_literature::storage::BIBTEX_DIR
// @item const:kovan_literature::storage::GENERATED_ROOT
// @item const:kovan_literature::storage::MARKDOWN_DIR
// @item const:kovan_literature::storage::OPEN_ROOT
// @item const:kovan_literature::storage::PROPRIETARY_ROOT
pub fn register(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_class::<Py_kovan_literature__BibEntry>()?;
    m.add_class::<Py_kovan_literature__BibParseError>()?;
    m.add_class::<Py_kovan_literature__Heading>()?;
    m.add_class::<Py_kovan_literature__LiteratureError>()?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__extract_assets, m)?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(fn_kovan_literature__extract_metadata, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__extract_pdf_text, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__markdown_outline, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__parse_bib_entries, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__pdf_to_markdown, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__render_entries, m)?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__render_entry, m)?)?;
    m.add_function(wrap_pyfunction!(
        fn_kovan_literature__split_markdown_by_page_limit,
        m
    )?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(
        fn_kovan_literature__storage__document_type_from_path,
        m
    )?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(
        fn_kovan_literature__storage__generated_dir_for,
        m
    )?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(fn_kovan_literature__storage__root_for, m)?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(
        fn_kovan_literature__storage__visibility_from_path,
        m
    )?)?;
    m.add_function(wrap_pyfunction!(fn_kovan_literature__text_to_markdown, m)?)?;
    #[cfg(feature = "kovan-common")]
    m.add_function(wrap_pyfunction!(fn_kovan_literature__to_bibtex, m)?)?;
    m.add("MAX_MARKDOWN_PAGES", ::kovan_literature::MAX_MARKDOWN_PAGES)?;
    m.add(
        "PAGE_SEPARATOR",
        ::kovan_literature::PAGE_SEPARATOR.clone().to_string(),
    )?;
    m.add(
        "ASSETS_DIR",
        ::kovan_literature::storage::ASSETS_DIR.clone().to_string(),
    )?;
    m.add(
        "BIBTEX_DIR",
        ::kovan_literature::storage::BIBTEX_DIR.clone().to_string(),
    )?;
    m.add(
        "GENERATED_ROOT",
        ::kovan_literature::storage::GENERATED_ROOT
            .clone()
            .to_string(),
    )?;
    m.add(
        "MARKDOWN_DIR",
        ::kovan_literature::storage::MARKDOWN_DIR
            .clone()
            .to_string(),
    )?;
    m.add(
        "OPEN_ROOT",
        ::kovan_literature::storage::OPEN_ROOT.clone().to_string(),
    )?;
    m.add(
        "PROPRIETARY_ROOT",
        ::kovan_literature::storage::PROPRIETARY_ROOT
            .clone()
            .to_string(),
    )?;
    Ok(())
}
